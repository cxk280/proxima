import { Shell } from "@/components/shell/Shell";
import { StubView } from "@/components/shell/StubView";

export default function LeaderboardPage() {
  return (
    <Shell fill>
      <StubView
        eyebrow="SUB-40MS ANYWHERE"
        title="The leaderboard is landing soon"
        blurb="Probes fired from a dozen global cities, every one landing under 40ms — with a single-region contrast mode. Ships in a later build."
      />
    </Shell>
  );
}
